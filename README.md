# dom-learning
to see your website, run the following in the terminal
`python -m httpserver`
if the link doesn't appear, go to PORTS, find the line with '8000', 

Inspection -> Console: > type: document
